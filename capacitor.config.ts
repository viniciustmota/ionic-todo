import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.mavellium.todoionic6',
  appName: 'Lista de Tarefas',
  webDir: 'www'
};

export default config;
